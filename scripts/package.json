{
  "name": "zeldtrade-scripts",
  "version": "1.0.0",
  "description": "Scripts utilitaires (newsletter, announce, etc.) — séparés du code applicatif",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "newsletter": "node send-newsletter.js"
  },
  "dependencies": {
    "nodemailer": "^9.0.1"
  }
}
